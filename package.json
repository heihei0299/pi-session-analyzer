{
  "name": "token-analyzer",
  "version": "2026.8.6-1",
  "description": "Analyze pi session JSONL files for token consumption statistics",
  "type": "module",
  "bin": {
    "token-analyzer": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/heihei0299/pi-session-anylize.git"
  },
  "keywords": [
    "pi",
    "token",
    "analyzer",
    "usage",
    "cost",
    "cli",
    "sessions"
  ],
  "scripts": {
    "test": "TZ=Asia/Shanghai node --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && cp src/webui.html dist/",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.7.0"
  }
}
