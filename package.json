{
  "name": "token-analyzer",
  "version": "2026.9.3",
  "description": "Analyze pi session JSONL files for token consumption statistics",
  "type": "module",
  "bin": {
    "token-analyzer": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/heihei0299/pi-session-anylize.git"
  },
  "keywords": [
    "pi",
    "token",
    "analyzer",
    "usage",
    "cost",
    "cli",
    "sessions"
  ],
  "scripts": {
    "test": "env -u TOKEN_ANALYZER_DB TZ=Asia/Shanghai node --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && cp internal/server/webui.html dist/",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.7.0"
  }
}
